# Cabide AI Configuration
# Copy this file to .env and fill in your values

# ===== REQUIRED =====

# Google Gemini API Key (Required for AI image generation)
# Get from: https://aistudio.google.com/app/apikey
# Note: Requires paid tier for production use
GEMINI_API_KEY=your_gemini_api_key_here

# ===== STORAGE CONFIGURATION =====

# Storage Mode: 'local' for development, 'prod' for Google Cloud Storage
# local = saves to ./outputs directory (for France/dev)
# prod = saves to GCS bucket (for Brazil/production)
STORAGE_MODE=local

# GCS Bucket Name (required when STORAGE_MODE=prod)
# Should be in southamerica-east1 (São Paulo) for Brazil deployment
GCS_BUCKET_NAME=cabide-catalog-br

# ===== GOOGLE DRIVE INTEGRATION (Optional) =====

# Google Drive Folder ID for automatic backup uploads
# Extract from Drive URL: https://drive.google.com/drive/folders/{FOLDER_ID}
# Leave empty to disable Drive integration
GDRIVE_FOLDER_ID=

# ===== GCP SERVICE ACCOUNT (Required for Production) =====

# Service Account JSON credentials (minified, single line)
# Required for both GCS storage and Google Drive integration in production
# To create:
#   1. Download JSON key from GCP Console
#   2. Minify it (remove newlines and extra spaces)
#   3. Paste the entire JSON as a single line here
# Example: {"type":"service_account","project_id":"your-project",...}
GCP_SERVICE_ACCOUNT_JSON={}

# ===== BACKEND API CONFIGURATION (Optional) =====

# Backend API URL (used by frontend in production/Docker mode)
# Leave empty for local development (frontend will use engine directly)
# In production, Cloud Run will inject this automatically
BACKEND_URL=

# ===== ADVANCED (Optional) =====

# Custom output directory for local mode (default: outputs)
# OUTPUT_DIR=outputs

# Custom temp upload directory (default: temp/uploads)
# TEMP_UPLOAD_DIR=temp/uploads

# Custom input directory for batch processing (default: inputs/clothes)
# INPUT_DIR=inputs/clothes
