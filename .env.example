# Cabide AI - Environment Configuration
# Copy this file to .env and fill in your values

# ===== REQUIRED =====

# Google Gemini API Key (Required for AI image generation)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ===== STORAGE CONFIGURATION =====

# Storage Mode: Always 'local' (saves to ./img/gen_images/output directory)
# GCS storage has been disabled - using local storage + Google Drive backup
STORAGE_MODE=local

# ===== GOOGLE DRIVE INTEGRATION (Optional) =====

# Google Drive Folder ID for automatic backup uploads
# Extract from Drive URL: https://drive.google.com/drive/folders/{FOLDER_ID}
# Leave empty to disable Drive integration
GDRIVE_FOLDER_ID=

# Service Account JSON credentials (minified, single line) - Optional
# Required ONLY if using Google Drive integration
# Alternative: Save credentials to gcp-service-account.json file (auto-detected)
# To create:
#   1. Download JSON key from GCP Console (IAM > Service Accounts)
#   2. Minify it (remove newlines and extra spaces)
#   3. Paste the entire JSON as a single line here OR save to gcp-service-account.json
# Example: {"type":"service_account","project_id":"your-project",...}
GCP_SERVICE_ACCOUNT_JSON=

# Email to impersonate when using Service Account with Google Drive
# Required if using domain-wide delegation with Service Account
GDRIVE_USER_EMAIL=

# ===== OAUTH CONFIGURATION (For Streamlit Cloud) =====

# OAuth Client Secret JSON (Web application type)
# Required ONLY for Streamlit Cloud deployment
# Get from: Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client ID
# Alternative: Save credentials to client_secret.json file (auto-detected for local dev)
# Format: Paste the entire JSON content from downloaded client_secret.json
# Example: {"web":{"client_id":"...","client_secret":"...","redirect_uris":[...]}}
CLIENT_SECRET_JSON=

# ===== BACKEND API CONFIGURATION (Optional) =====

# Backend API URL (used by frontend in production/Docker mode)
# Leave empty for local development (frontend will use engine directly)
# Example for production: https://your-backend.run.app
BACKEND_URL=
